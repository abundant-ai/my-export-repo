openapi: 3.0.0
info: { title: t, version: 2.0.0 }
paths:
  /search:
    get:
      parameters:
        - in: query
          name: q
          required: false
          schema: { type: string }
      responses:
        "200": { description: ok }
