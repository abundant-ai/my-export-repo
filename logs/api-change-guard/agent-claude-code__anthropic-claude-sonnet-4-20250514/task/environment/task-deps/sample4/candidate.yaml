openapi: 3.0.0
info: { title: t, version: 1.1.0 }
paths:
  /users/{id}:
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200": { description: ok }
