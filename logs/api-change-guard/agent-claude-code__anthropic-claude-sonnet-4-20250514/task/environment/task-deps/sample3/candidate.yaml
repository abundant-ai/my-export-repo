openapi: 3.0.0
info: { title: t, version: 2.0.1 }
paths:
  /search:
    get:
      parameters:
        - in: query
          name: q
          required: true
          schema: { type: string }
      responses:
        "200": { description: ok }
